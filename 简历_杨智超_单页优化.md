# 杨智超 · 单页简历优化稿（后端开发 / AI 应用开发）

> 目标：压回 1 页 A4。做法 = 格式收紧（主因）+ 少量内容精简（保险）。
> 注：原文里 "ai 应用开发" 已统一为 "AI 应用开发"；原自我评价是套话，已替换为有信息量的版本。

---

## 一、最终单页文本（直接替换你 Word 里的内容）

杨智超
求职意向：后端开发 / AI 应用开发 ｜ 广东 ｜ 17260185375 ｜ 1768305354@qq.com

**教育背景**
2023.09 – 至今　广东理工学院　软件工程（本科）　专业前 15%

**荣誉证书**
HCIA-HarmonyOS 应用开发认证；CET-4；全国计算机等级考试二级

**竞赛经历**
2025 广东省大学生计算机设计大赛 · 三等奖（省级，AI 视觉应用创新挑战赛）
基于机器视觉的布料缺陷检测系统 ｜ 核心成员 ｜ YOLOv8 + Python + OpenCV + ECharts
- 制定 8 类缺陷标注规范，LabelImg 完成 2000+ 张标注；YOLOv8 训练调优（batch=16 / lr=0.01 / epochs=100，Mosaic + 余弦退火），mAP@0.5 达 92.3%。
- HTML + ECharts 可视化看板展示缺陷分布 / 严重程度并支持 CSV 导出；参与答辩路演与方案讲解。

2025 RAICOM 睿抗机器人开发者大赛校内选拔赛 · 三等奖（AI 视觉应用赛）
基于 YOLO 的食物热量识别与饮食建议系统 ｜ 核心成员 ｜ YOLOv8 + FastAPI + 大模型 API + ECharts
- 20+ 类别食品图像采集标注 3000+ 张，YOLOv8 微调优化小目标 anchor，Top-1 准确率 89.5%；Web 实时识别（名称 + 置信度）。
- 对接千问 / DeepSeek 大模型 API，将识别结果注入 Prompt 估算热量并生成饮食建议；负责架构图 / 文档 / 答辩。

**项目经历**
2025.04 – 2025.05　AI Agent 智能客服工单系统 ｜ 全栈独立开发
FastAPI + LangChain + LangGraph + ChromaDB + SQLAlchemy 2.0 + JWT
- 基于 RAG 架构打通「文档上传 → 语义检索 → LLM 问答 → 自动建单 → 状态流转」全链路闭环。
- LangChain 递归切片 + Sentence-Transformers 向量化存入 ChromaDB（HNSW + 余弦相似度 Top-K=3）；LangGraph 构建工单状态机（4 状态 + 5 边）强制校验与审计；LLM 以 JSON 结构化输出建单决策。
- 工程落地：JWT（python-jose + bcrypt）多用户隔离、SSE 流式对话（5 事件）、SQLAlchemy 2.0、Docker 部署。

2026.05 – 2026.06　宠物领养信息平台 ｜ 全栈开发
Spring Boot 3.2.5 + Vue 3 + MyBatis-Plus + Redis + MySQL
- 基于 Spring Boot 3.2.5（Java 17）搭建 Maven 多模块后端（common / repository / service / infrastructure / api），设计「枚举 + 事件驱动」领养状态机，@Transactional 保障状态流转与记录 / 通知的事务一致性。
- Spring Security + JWT 实现无状态认证与三角色 RBAC；MyBatis-Plus 动态 SQL + MySQL 联合索引 + Redis 缓存完成多条件检索（P95 < 500 ms），ShedLock 保证回访定时任务幂等。
- 工程规范：Flyway 迁移、MapStruct 映射、SpringDoc 自动 Swagger、JWT 登出 Redis 黑名单。

**专业技能**
语言：Java（主力）/ Python / SQL / ArkTS(了解)　｜　后端：Spring Boot、MyBatis-Plus、FastAPI　｜　数据：MySQL、Redis、ChromaDB　｜　AI/LLM：LangChain、LangGraph、RAG、YOLO、Prompt 工程　｜　工具：Docker、Git、Maven、Postman　｜　前端：Vue.js(了解)、HTML/CSS/JS　｜　其他：RESTful API、JWT、SSE、Linux

**自我评价**
计算机专业应届生，具备「后端开发 + AI 应用落地」复合能力：后端熟练 Spring Boot / MyBatis-Plus / Redis，AI 拥有 RAG（LangChain + LangGraph + ChromaDB）与 YOLO 实战经验；两次省级竞赛围绕视觉与大模型展开，学习主动、文档规范、乐于协作。

---

## 二、Word 一页化设置（最关键，先做这个）

1. **页边距**：布局 → 页边距 → 自定义，上下左右全部改为 **1.6–1.8 cm**（默认 2.54 cm 太宽，改完直接省出大半页）。
2. **字号**：正文 **10.5 pt（五号）**，板块标题 12 pt 加粗；中文用「微软雅黑」或「宋体」，英文 / 数字用「Calibri」。
3. **行距**：选中全文 → 段落 → 行距选「固定值」**14–15 pt**（约 1.05–1.1 倍，别用 1.5 倍）。段前 / 段后都设 **0 pt**（最多 2 pt）。
4. **去空行**：板块之间只留 1 个空行，删掉多余的回车。
5. **打印预览**：Ctrl + P 看是否 1 页。若仍多一点点，把「专业技能」那行再压短，或把「荣誉证书」并入教育背景行。

> 实操顺序：先只改「页边距 + 字号 + 行距」三步，大概率已经回到 1 页；上面的内容精简是双保险 + 顺手提质。

---

## 三、相比原稿做了哪些删改（透明列示）

- ✂️ 删 **主修课程**整段（8 门课占 2 行，对后端 / AI 岗非必要，留「专业前 15%」更值钱）。
- ✂️ 头部 **年龄 / 性别 / 工作年限 / 政治面貌** 去掉，只留姓名 + 求职意向 + 电话 + 邮箱（一行搞定）。
- ✂️ **荣誉证书** 由 3 行 → 1 行。
- ✂️ **竞赛** 各 3 条 → 2 条（合并了过程性描述，量化结果 92.3% / 89.5% / 2000+ / 3000+ 全保留）。
- 🔁 **自我评价** 换成有信息量的复合能力版（原稿是「积极认真、细心负责」套话，面试官秒跳过）。
- 🔁 **专业技能** 由 9 行子弹点 → 1 行分类短语，避免碎行撑出第二页。
- 🔤 "ai 应用开发" → "AI 应用开发"。

> 若你更想保留竞赛 3 条原样，只要把「页边距 + 字号 + 行距」三步走完，原稿通常也能压回 1 页——内容不是溢出的主因。
