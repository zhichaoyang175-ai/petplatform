# 简历优化 · 杨智超（后端开发 / AI 应用开发）

> 说明：源 PDF 在本环境无法解析文本，以下基于你粘贴的「竞赛经历」+ 已完成的「项目经历」(PetPlatform / AI Agent) 优化。如需完整版评分，把整份简历文字贴给我即可。

---

## 项目经历（缩短版，3 条 bullet，可直接替换原文）

### AI Agent 智能客服工单系统（AI 应用开发岗主线）
角色：全栈独立开发 ｜ FastAPI + LangChain + LangGraph + ChromaDB + SQLAlchemy 2.0 + JWT
- 全栈独立开发基于 RAG 架构的智能客服工单系统，打通「文档上传 → 语义检索 → LLM 问答 → 自动建单 → 状态流转」全链路闭环，替代传统关键词规则匹配。
- 基于 LangChain 递归切片 + Sentence-Transformers 向量化写入 ChromaDB（HNSW + 余弦相似度 Top-K=3 检索）；LangGraph 构建工单状态机（4 状态 + 5 边）强制状态校验与审计日志；LLM 分析对话上下文以 JSON 结构化输出建单决策。
- 工程落地：JWT（python-jose + bcrypt）多用户隔离、SSE 流式对话（5 种事件）、SQLAlchemy 2.0 持久化、Docker 容器化部署。

### PetPlatform 宠物领养信息平台（后端开发岗主线）
角色：全栈开发 ｜ Spring Boot 3.2.5 + Vue 3 + MyBatis-Plus + Redis + MySQL
- 基于 Spring Boot 3.2.5（Java 17）搭建 Maven 多模块模块化单体后端（common / repository / service / infrastructure / api），设计「枚举 + 事件驱动」领养状态机，以 @Transactional 保障状态流转、记录写入与消息通知的事务一致性。
- 基于 Spring Security + JWT 实现无状态认证与三角色 RBAC；使用 MyBatis-Plus 动态 SQL + MySQL 联合索引 + Redis 缓存完成宠物多条件检索（P95 < 500 ms 设计目标），ShedLock 保证回访定时任务幂等。
- 落地工程规范：Flyway 数据库迁移、MapStruct DTO 映射、SpringDoc 自动 Swagger、JWT 登出 Redis 黑名单，提升接口可观测性与可维护性。

> 缩短点：AI 项目原 5 条 → 3 条；PetPlatform 后端版原 5 条 → 3 条。完整细节见 `AI_Agent_智能客服工单系统_简历项目描述.md` 与 `PetPlatform_简历项目描述.md`。

---

## 一、竞赛经历（缩短版，可直接替换原文）

### 竞赛一：基于机器视觉的布料缺陷检测系统
**获奖：2025 年广东省大学生计算机设计大赛 · 三等奖**（AI 视觉应用创新挑战赛，省级） ｜ 角色：核心成员 ｜ YOLOv8 + Python + OpenCV + ECharts
- 制定 8 类布料缺陷（破洞 / 污渍 / 色差 / 褶皱等）标注规范，使用 LabelImg 完成 2000+ 张图像标注与数据集预处理。
- 基于 YOLOv8 训练调优（batch_size=16, lr=0.01, epochs=100，Mosaic 增强 + 余弦退火），mAP@0.5 达 92.3%，并据验证集指标动态调整防过拟合。
- 使用 HTML + ECharts 开发检测可视化看板，实时展示缺陷分布 / 严重程度并支持 CSV 导出；参与答辩路演与技术方案讲解。

### 竞赛二：基于 YOLO 的食物热量识别与饮食建议系统
**获奖：2025 年睿抗机器人开发者大赛 (RAICOM) 校内选拔赛 · 三等奖**（AI 视觉应用赛） ｜ 角色：核心成员 ｜ YOLOv8 + FastAPI + 大模型 API + ECharts
- 参与 20+ 类别食品图像采集与标注，完成 3000+ 张样本，基于 YOLOv8 预训练权重微调并优化小目标 anchor，Top-1 识别准确率 89.5%。
- 开发 Web 上传与实时识别页面（食物名称 + 置信度）；对接千问 / DeepSeek 大模型 API，将识别结果注入 Prompt 估算总热量并生成个性化饮食建议。
- 负责系统架构图绘制、技术文档撰写与答辩汇报。

> 缩短点：原 5 条 → 3 条，砍掉冗余的过程描述（如「监控 loss 曲线」「参与 PPT 制作」并入结果/答辩句），保留量化结果（92.3% / 89.5% / 2000+ / 3000+）。

---

## 二、自我评价（新增，建议放在教育背景之后 / 项目之前）

计算机相关专业应届生，具备「后端开发 + AI 应用落地」的复合能力。后端方向熟练使用 Spring Boot 3 / MyBatis-Plus / MySQL / Redis 进行模块化架构设计与 RESTful API 开发，重视事务一致性与工程规范；AI 方向拥有 RAG（LangChain + LangGraph + ChromaDB）与 YOLO 视觉模型的实战经验，能独立完成「文档检索问答 → 自动建单」「图像识别 → 大模型生成建议」等端到端应用。两次竞赛均围绕计算机视觉与大模型展开，具备数据标注、模型调优与答辩汇报能力。学习主动、文档规范、乐于协作，期望在后端 / AI 应用开发岗位持续成长。

---

## 三、整体优化建议（按优先级）

### 🔴 关键（不补会影响通过率）
1. ~~**竞赛获奖信息空缺**~~ ✅ 已补全：
   - 竞赛一：**2025 年广东省大学生计算机设计大赛 · 三等奖**（省级，广东省教育厅颁发，AI 视觉应用创新挑战赛）
   - 竞赛二：**2025 年 RAICOM 睿抗机器人大赛校内选拔赛 · 三等奖**（AI 视觉应用赛）
2. **AI 项目周期缺失**：上轮 `AI_Agent_智能客服工单系统_简历项目描述.md` 里周期是占位符，需填实际上下限。

### 🟡 重要（提升匹配度）
3. **双赛道关键词都要覆盖**：简历整体应包含——后端组（Spring Boot / MyBatis-Plus / Redis / JWT / 事务 / 微服务）与 AI 组（LangChain / LangGraph / RAG / ChromaDB / YOLO / LLM / 大模型 API），让 ATS 在两条赛道都吃到词。
4. **角色措辞统一**：竞赛写「核心成员」、项目写「全栈独立开发」没问题，但要确保两者在简历中区分清晰，别让面试官误以为项目也是「参与」。
5. **项目经历也可补硬指标**：竞赛有 mAP / 准确率，项目也可加类似数字（模块数、表数量、接口数、缓存命中率等），避免项目描述偏「过程」缺「结果」。

### 🟢 次要（排版与阅读）
6. **板块顺序建议**：教育背景 → 自我评价 → 项目经历（PetPlatform + AI Agent）→ 竞赛经历 → 专业技能 / 证书。竞赛放项目之后，避免「竞赛盖过项目」。
7. **单列「专业技能」模块**：分 后端 / AI / 前端 / 工具 四类罗列，便于 HR 6 秒扫描与 ATS 抓取。
8. **求职意向写明**：顶部明确「求职意向：后端开发 / AI 应用开发」，与文件名一致。

### 💡 建议
9. **基础信息核查**：确认含 姓名、电话、邮箱、GitHub / 博客、所在地；文件名「应届生年经验」疑似漏字（应为「应届生 0 年经验」），定稿前统一。
10. **真实性兜底**：mAP@0.5=92.3%、Top-1=89.5%、标注张数等面试必被追问训练细节与数据来源，确保能讲清。

---

## 四、可直接合并的简历结构（推荐）

1. 基本信息 + 求职意向（后端 / AI 应用开发）
2. 教育背景
3. 自我评价（见第二节）
4. 项目经历
   - AI Agent 智能客服工单系统（AI 岗主线）
   - PetPlatform 宠物领养信息平台（后端岗主线）
5. 竞赛经历（见第一节缩短版）
6. 专业技能（后端 / AI / 前端 / 工具 四类）
7. 证书 / 其他（如有）
